<%= simple_form_for(@deployment) do |f| %>

	<%= display_errors!(@deployment) %>

	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :author_name %>
	<%= f.input :author_email %>

	<%= btn_container do %>
		<%= f.button :submit, :class => "btn-primary" %>
	<% end %>
<% end %>
